Angular 2 Client & Library - Dynamic Components
===============================================

Building
==============

Library:

- cd <root dir>/library
- npm install
- npm run clean-build:aot

Client:

- cd <root dir>/client
- npm install
- npm run clean-build:aot
